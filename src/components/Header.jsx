import SocialButtons from './SocialButtons'

const Header = () => (
  <header className="bg-primary text-white p-4 sticky top-0 z-10">
    <SocialButtons className="justify-evenly" />
  </header>
)

export default Header
